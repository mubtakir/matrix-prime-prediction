# 📦 متطلبات نظام التنبإ بالأعداد الأولية المصفوفي
# Matrix Prime Prediction System Requirements

# المتطلبات الأساسية (Core Requirements)
# لا توجد مكتبات خيرجية مطلوبة للوظائف الأساسية
# النظام يعتمد على مكتبات Python المدمجة فقط

# المتطلبات الاختيارية للميزات المتقدمة (Optional Requirements)

# لاختبارات الأداء والرسوم البيانية
matplotlib>=3.5.0
numpy>=1.21.0

# لمراقبة استهلاك الذاكرة
psutil>=5.8.0

# للاختبارات المتقدمة
pytest>=6.2.0
pytest-benchmark>=3.4.0

# للتوثيق (اختياري)
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0

# لتحليل الكود (X�ختياري)
flake8>=4.0.0
black>=22.0.0
mypy>=0.910

# للتطوير (X�ختياري)
jupyter>=1.0.0
ipython>=7.0.0

# ملاحظات
# - النظام الأساسي يعمل بدون أي مكتبات خيرجية
# - المتطلبات الاختياريي تضيف ميزات متقدمة فقط
# - يمكن تثبيت  المتطلبات الأساسية فقط باستخدام:
#   pip install matplotlib psutil
